# Starter Messages

- `请使用 paper-framework-figure-studio-pro skill，根据当前状态和已登记产物，判断当前 step；如果这只是首次总目标请求，不要执行 S0，只给出进入 S0-PAPER-FOUNDATION 的可复制提示词；如果用户明确要求执行某个 step，只执行本轮用户明确要求的一个 step，完成后停下并给出下一步可复制提示词。`
- `如果不知道如何提问，请说：请使用 paper-framework-figure-studio-pro 根据当前状态只建议下一步提示词，不要自动执行下一步`
- `额外说明这个 skill 的设计初衷是什么。`
- `请使用 paper-framework-figure-studio-pro skill，根据当前状态和已登记产物，进入 S0-PAPER-FOUNDATION。本轮纯文字：只执行文字、state、manifest、brief、audit、guidance 或 checkpoint 写入；不要生成任何图片。请建立项目状态、确认运行环境、输入材料、画幅和候选数量默认值；如果输入包含 PDF/LaTeX/论文正文/详细方法描述，请只基于允许读取的论文内容生成 paper-foundation-report.md 和 framework-figure-risk-register.md。完成 S0 后停止，只给出 S1-FIGURE-STRATEGY 的可复制提示词。`
- `请使用 paper-framework-figure-studio-pro skill，根据当前状态和已登记产物，进入 S1-FIGURE-STRATEGY。本轮纯文字：只执行文字、state、manifest、brief、audit、guidance 或 checkpoint 写入；不要生成任何图片。请根据 S0 foundation 和 risk register 完成图策略，并在 S1 内部完成 S2 生图所需的 prompt packages、candidate registry、layout/routing/edge contracts、prompt-index 和 image-only handoff；对每个生图 prompt 执行严格契约审核与最多 3 次修复循环，确保所有箭头/连线有论文证据、两个 block 间默认只保留一条合并连接线（除非不同线携带不同且有证据的传递量）、跨模块变量写在线/port/tag 上、模块化不碎片化、内部示意图简洁通用、无重复 workflow、背景只占小部分。第一轮默认风格：正式出版风格；请在 S1 结束的 S2 handoff 前提醒用户：若要在 S2 生图前修改第一轮默认风格，可在下一步提示词中改为正式出版风格、低保真草图、白板线框、干净扁平极简线稿、正式 schematic 布局草案、轻量蓝图精密稿、轻量科学插画、轻量界面隐喻、轻量等距结构、轻量信息图板或手绘故事板。完成 S1 后停止；不要生成 S2 图像。`
- `请使用 paper-framework-figure-studio-pro skill，根据当前状态和已登记产物，进入并只执行 S2-SKETCH-EXPLORE 的 IMAGE_GENERATE。第一轮默认风格：正式出版风格；如果 S1 prompt-index/prompt package 中记录了用户显式修改的兼容第一轮风格，则按该显式覆盖执行。请逐一读取 S1 已保存的 prompt-index 中每个 candidate 的 prompt_path，用当前 runtime 锁定的图像生成路线生成第一轮候选图，并保持 prompt-index 中 candidate_id、prompt_path、target_image_path、状态、artifact 和 checkpoint 路径一致。禁止 SVG、Python/PIL、Matplotlib、Graphviz、TikZ、Mermaid、canvas、PPT/PDF、截图或本地程序化 PNG/WebP。不要写审核、解释、排序、修改建议或下一步文字。`
- `请使用 paper-framework-figure-studio-pro skill，根据当前状态和已登记产物，进入 S3-DIRECTION-SELECT。本轮纯文字：只执行文字、state、manifest、brief、audit、guidance 或 checkpoint 写入；不要生成任何图片。请先在 S3 内部完成 S2 图像 review 与 exploration aggregate，再基于论文事实、S1策略和 S2视觉信号选择方向；用户可在本提示中指定倾向的一个或多个第一轮候选图 ID 作为参考信号；如指定，S3 必须记录为 user_preferred_first_round_candidate_ids 并传给 S4 作为覆盖义务，但 S3 仍需基于证据和 issue-ledger 选择方向；完成后停止，只给出 S4-CANDIDATE-BRIEF 的下一步提示词。`
- `请使用 paper-framework-figure-studio-pro skill，根据当前状态和已登记产物，进入 S4-CANDIDATE-BRIEF。本轮纯文字：只执行文字、state、manifest、brief、audit、guidance 或 checkpoint 写入；不要生成任何图片。请按 S3 方向生成正式候选矩阵、S2 风险转移、图文分工、元素布局合同、routing/arrow/port 合同、visible text whitelist、line-carried variable registry、internal visual motif plan；如果 S3 记录了用户偏好的第一轮候选 ID，则必须为每个偏好来源 × 每个 S5 风格/处理槽生成至少 1 个 preference-led local-essence S5 候选，并在 S4 内部完成 S5 生图所需的 prompt packages、prompt-index 和 image-only handoff；对每个生图 prompt 执行严格契约审核与最多 3 次修复循环，确保所有箭头/连线有论文证据、两个 block 间默认只保留一条合并连接线（除非不同线携带不同且有证据的传递量）、跨模块变量写在线/port/tag 上、模块化不碎片化、内部示意图简洁通用、无重复 workflow、背景只占小部分。完成 S4 后停止；不要生成 S5 图像。`
- `请使用 paper-framework-figure-studio-pro skill，根据当前状态和已登记产物，进入并只执行 S5-CANDIDATE-IMAGE 的 IMAGE_GENERATE。请逐一读取 S4 已保存的 prompt-index 中每个 candidate 的 prompt_path，用当前 runtime 锁定的图像生成路线生成正式 paper-framework raster candidates，并保持 prompt-index 中 candidate_id、prompt_path、target_image_path、状态、artifact 和 checkpoint 路径一致；不要把 F01-F06 或 prompt-index 中的其他 ID 改写成 C01-C06 或数字序号。禁止 SVG、Python/PIL、Matplotlib、Graphviz、TikZ、Mermaid、canvas、PPT/PDF、截图或本地程序化 PNG/WebP。不要写审核、解释、排序、修改建议或下一步文字。S5 生图后 workflow 结束。`
- `我的任务已经完成，剩下由人类来决策。`
